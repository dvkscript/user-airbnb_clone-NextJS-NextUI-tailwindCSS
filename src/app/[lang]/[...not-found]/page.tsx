import NotFound from '@/components/Common/NotFound'
import React from 'react'

const NotFoundPage = () => {
    return (
        <NotFound className='h-screen w-screen' />
    )
}

export default NotFoundPage

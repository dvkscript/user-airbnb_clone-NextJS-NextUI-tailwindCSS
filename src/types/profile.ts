export interface Profile {
    id: string,
    gender: string | null,
    address_id: string | null,
    photo_id: string | null,
    user_id: string | null,
    created_at: string,
    updated_at: string,
}